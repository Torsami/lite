const votesDown = (err, res) => {
    const id = parseInt(req.params.questionId, 10);
    const answer = parseInt(req.params.answerId, 10);
    db.map((question) => {
        if(question.id === id){

        }
    }

}